---
home_path: "{{ lookup('env', 'HOME') }}"
conf_path: "{{ home_path }}/.config"

golang_path: "{{ home_path }}/Projects/go-workspace"

nash_root: "{{ home_path }}/nashroot"
nash_path: "{{ home_path }}/nash"
